# Chromeウェブストア 公開・設定マニュアル

このドキュメントでは、HideAccessCheckerをChromeウェブストアにアップロードし、社内限定で公開・配布する手順を解説します。

## 手順1：拡張機能のパッケージ化（ZIP作成）

まず、アップロード用のファイルを作成します。

1. PC上の `HideAccessChecker` フォルダを開きます。
2. 中身（`manifest.json`、`content.js`、`images` フォルダ）がすべて揃っていることを確認します。
3. `HideAccessChecker` フォルダそのものを右クリックし、「圧縮（ZIPファイルにする）」を選択します。
4. `HideAccessChecker.zip` というファイルができます。これが提出物です。

## 手順2：Chromeウェブストアへのアップロード

1. [Chrome Web Store Developer Dashboard](https://chrome.google.com/webstore/devconsole/) にアクセスします。
2. 右上の **「新しいアイテム」** をクリックします。
3. 先ほど作成した `HideAccessChecker.zip` をドラッグ＆ドロップします。

## 手順3：基本情報の入力（ストアの掲載情報タブ）

左メニューの「ストアの掲載情報」での入力項目です。社内用でも入力が必須です。

*   **名前・概要**: `manifest.json` から自動入力されています（そのままでOK）。
*   **カテゴリ**: 「仕事効率化」などを選択。
*   **言語**: 「日本語」を選択。
*   **アイコン**: 128x128 形式のアイコン画像（`images/icon128.png`）をアップロードします。
*   **スクリーンショット**: 最低1枚必要です。「ダイアログで自動選択されている画面」のキャプチャ（1280x800px推奨）をアップロードしてください。

## 手順4：プライバシー設定（プライバシータブ）

左メニュー「プライバシー」タブの設定です。

*   **単一用途**:
    > GmailでGoogleドライブのファイルを添付して送信する際、アクセス権確認ダイアログにおいて、誤って不要な共有権限を付与しないよう、「アクセス権を付与しない」オプションを自動的に選択し、セキュリティ事故を防止するため。
*   **権限が必要な理由**:
    *   **scripting**: Gmailのメール作成画面およびその中で読み込まれるフレーム（iframe）内において、アクセス権限確認ダイアログの表示を検知し、特定のラジオボタン（アクセス権を付与しない）を自動的に選択するスクリプトを実行するために必要です。
    *   **ホスト権限**: 拡張機能の動作対象であるGmail（mail.google.com）および、ダイアログの実体として読み込まれるGoogleドライブやドキュメント（drive.google.com, docs.google.com, calendar.google.com）のページ上でのみスクリプトを動作させ、DOM要素を監視・操作するために必要です。ユーザーの閲覧履歴等の取得は行いません。
*   **リモートコード**: 「いいえ」
*   **データ使用（チェックボックス）**: すべてチェックなし（空欄）。
*   **コンプライアンス表明**: 3つすべてにチェックを入れる。
*   **プライバシーポリシーURL**: `https://www.master-apps.jp/privacy-policy/`

## 手順5：社内限定に設定（販売地域タブ）

**ここが最も重要です。** 一般公開しないための設定を行います。

1. 左メニューの **「販売地域」**（または「配布」）タブをクリックします。
2. **「公開設定」** という項目を探します。
3. **「プライベート」** を選択します。
4. その下に表示される **「[御社のドメイン名] の全員」** を選択します。
    * これにより、会社のGoogleアカウントでログインしている人だけがこの拡張機能を見たりインストールしたりできるようになります。
    * ※「リンクを知っている人のみ」を選ぶと、外部の人でもURLを知っていればインストールできてしまうため、「ドメイン内の全員」が最も安全です。

## 手順6：審査への提出

1. すべてのタブで「保存」ができていることを確認します。
2. 右上の **「審査のため送信」** ボタンをクリックします。
3. 確認画面が出るので、そのまま送信します。

> [!NOTE]
> **審査期間について**
> 社内限定（プライベート）公開の場合、通常の公開よりも審査が早く終わる傾向があります（早ければ数時間〜1日程度）。

---

## （オプション）社内PCへの強制インストール

審査が通り「公開」状態になった後、管理者が自動的に配布する方法です。

1. [Google 管理コンソール](https://admin.google.com/) にログインします。
2. **デバイス > Chrome > アプリと拡張機能 > ユーザーとブラウザ** に移動します。
3. 右下の「＋」ボタン > **Chromeウェブストアから追加** を選びます。
4. 今回登録した拡張機能の **ID**（URLの末尾の長い英数字）を入力して検索・追加します。
5. インストールポリシーを **「強制インストール」** に設定して保存します。

これで、社員がChromeを開くと自動的にこの拡張機能が入り、使えるようになります。
